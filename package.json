{
  "name": "vendor-scout",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch server.mjs",
    "start": "node server.mjs",
    "demo:decision": "node scripts/demo-decision.mjs",
    "test": "node --test",
    "check": "node --check server.mjs && node --check public/app.js && node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
